import type { MainPageState } from '~pages/functions';

function SwapPage({ setState }: { setState: (state: MainPageState) => void }) {
    return <div className="w-full">Swap</div>;
}

export default SwapPage;
